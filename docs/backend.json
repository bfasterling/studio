{
  "entities": {
    "Document": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Document",
      "type": "object",
      "description": "Represents a document uploaded to the system for analysis.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Document entity."
        },
        "fileName": {
          "type": "string",
          "description": "The original file name of the uploaded document."
        },
        "fileType": {
          "type": "string",
          "description": "The type of the uploaded document (e.g., 'pdf', 'text')."
        },
        "uploadDate": {
          "type": "string",
          "description": "The date and time when the document was uploaded.",
          "format": "date-time"
        },
        "content": {
          "type": "string",
          "description": "The textual content of the document, extracted for analysis."
        }
      },
      "required": [
        "id",
        "fileName",
        "fileType",
        "uploadDate",
        "content"
      ]
    },
    "AnalysisInstruction": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "AnalysisInstruction",
      "type": "object",
      "description": "Represents the instructions provided by the administrator for analyzing documents.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the AnalysisInstruction entity."
        },
        "instructionText": {
          "type": "string",
          "description": "The text of the analysis instruction."
        },
        "creationDate": {
          "type": "string",
          "description": "The date and time when the analysis instruction was created.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "instructionText",
        "creationDate"
      ]
    },
    "DocumentAnalysis": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "DocumentAnalysis",
      "type": "object",
      "description": "Represents the result of analyzing a document based on specific instructions.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the DocumentAnalysis entity."
        },
        "documentId": {
          "type": "string",
          "description": "Reference to Document. (Relationship: Document 1:N DocumentAnalysis)"
        },
        "analysisInstructionId": {
          "type": "string",
          "description": "Reference to AnalysisInstruction. (Relationship: AnalysisInstruction 1:N DocumentAnalysis)"
        },
        "analysisResult": {
          "type": "string",
          "description": "The result of analyzing the document based on the provided instructions."
        },
        "analysisDate": {
          "type": "string",
          "description": "The date and time when the document analysis was performed.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "documentId",
        "analysisInstructionId",
        "analysisResult",
        "analysisDate"
      ]
    },
    "Conversation": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Conversation",
      "type": "object",
      "description": "Represents a question/answer pair in a conversation between a user and the AI agent.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Conversation entity."
        },
        "userId": {
          "type": "string",
          "description": "The user id to identify which user initiated the conversation turn."
        },
        "questionText": {
          "type": "string",
          "description": "The text content of the user's question."
        },
        "answerText": {
          "type": "string",
          "description": "The text content of the AI's answer."
        },
        "timestamp": {
          "type": "string",
          "description": "The date and time when the conversation turn was recorded.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "userId",
        "questionText",
        "answerText",
        "timestamp"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/documents/{documentId}",
        "definition": {
          "entityName": "Document",
          "schema": {
            "$ref": "#/backend/entities/Document"
          },
          "description": "Stores all user-uploaded documents in a single global collection.",
          "params": [
            {
              "name": "documentId",
              "description": "The unique ID of the document."
            }
          ]
        }
      },
      {
        "path": "/analysis_instructions/{analysisInstructionId}",
        "definition": {
          "entityName": "AnalysisInstruction",
          "schema": {
            "$ref": "#/backend/entities/AnalysisInstruction"
          },
          "description": "Stores analysis instructions provided by the administrator.",
          "params": [
            {
              "name": "analysisInstructionId",
              "description": "The unique ID of the analysis instruction."
            }
          ]
        }
      },
      {
        "path": "/documents/{documentId}/analyses/{analysisId}",
        "definition": {
          "entityName": "DocumentAnalysis",
          "schema": {
            "$ref": "#/backend/entities/DocumentAnalysis"
          },
          "description": "Stores the results of analyzing documents based on specific instructions.",
          "params": [
            {
              "name": "documentId",
              "description": "The ID of the document being analyzed."
            },
            {
              "name": "analysisId",
              "description": "The unique ID of the document analysis."
            }
          ]
        }
      },
      {
        "path": "/conversations/{conversationId}",
        "definition": {
          "entityName": "Conversation",
          "schema": {
            "$ref": "#/backend/entities/Conversation"
          },
          "description": "Stores all conversation turns (Q&A pairs) in a single global collection.",
          "params": [
            {
              "name": "conversationId",
              "description": "The unique ID of the conversation turn."
            }
          ]
        }
      }
    ],
    "reasoning": "The Firestore structure is designed to support the DocuChat AI application as a shared knowledge base. All documents and chat messages are stored in global collections to be accessible by any user, promoting a collaborative environment. This removes the need for user-specific data paths. \n\n**Authorization Independence:** Documents and chat messages are in top-level collections (`/documents`, `/chat_messages`). Access is controlled globally for all authenticated users.\n\n**QAPs (Rules are not Filters):** Security rules will allow any authenticated user to read and write to the shared collections, which is efficient and secure for this public-access model."
  }
}
